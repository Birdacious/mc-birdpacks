# as @e[tag=timestopeffector, tag=detectground] at @s

# set to expire early because nautilus particles take a little while to fully disappear
execute if score @s boomtimer matches ..360 run particle minecraft:nautilus ~ ~1 ~ 0 0 0 5 8 force